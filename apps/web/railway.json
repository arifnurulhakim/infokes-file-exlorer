{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && bun install && cd apps/web && bun run build"
  },
  "deploy": {
    "startCommand": "bunx serve -s dist -l $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
