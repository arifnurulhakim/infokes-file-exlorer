{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && bun install"
  },
  "deploy": {
    "startCommand": "bun run db:migrate && bun run src/index.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
